# Publication hold

This directory is a deterministic release candidate, not publication authority. Complete clean-clone verification and obtain a separate explicit owner decision before any Git remote, push, upload, release, or Pages deployment.

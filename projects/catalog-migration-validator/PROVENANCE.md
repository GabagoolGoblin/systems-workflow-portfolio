# Provenance

This clean-room project contains original standard-library Python and invented source, target, and mapping fixtures. No employer/customer catalog, vendor export, product screenshot, credential, private schema, or production identifier was used or copied.

All tool I/O is local and caller-directed. There is no browser automation, network adapter, telemetry, account, or credential path. Tests isolate their writes in temporary directories.

The root `LICENSE` governs cleared owner-created material. Generated migration artifacts are excluded from the release inventory and must never be populated with private or production records in this repository.

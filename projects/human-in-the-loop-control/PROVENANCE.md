# Provenance

This is a newly selected clean-room carve-out of executable, independently authored portfolio code. It includes only Python/Tk source, a bundled synthetic fixture, deterministic tests, and a fresh code-native HTML/CSS/JavaScript preview authored for the public capability-first repository.

No historical screenshot, generated image, image metadata, employer/customer record, application material, private path, product interface, brand asset, or old repository history is included. The public preview was designed from the checked-in synthetic state model.

Every fixture record and interface value is invented. Runtime imports are Python standard-library or repository-local; Tk is an operating-system GUI binding. The browser preview has no remote dependency.

Owner-created material is governed by the common root `LICENSE`, and its clean-room provenance is recorded in `release-decisions.json`.

# Provenance

This clean-room portfolio artifact contains original HTML, CSS, JavaScript, Python, tests, and deterministic JSON. Candidate names, task briefs, outputs, scores, rationales, programs, and events are invented. No private prompt, real model output, customer record, provider asset, copied screenshot, logo, trade dress, or proprietary product behavior is included.

The reference receipt and two browser data bundles derive only from the checked-in synthetic fixtures. `tools/release/verify_generated.py` rebuilds them in a temporary copy and compares exact bytes. The selected hero is a local Firefox render of the checked-in interface and retains the permanent no-authority boundary.

Runtime dependencies are a current browser and Python standard library. Playwright/Firefox is verification-only. The root `LICENSE` governs owner-created material; this provenance record does not establish production or employment history.
